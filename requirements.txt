pandas>=2.0
numpy>=1.24
pyarrow>=12.0
matplotlib>=3.7
mplfinance>=0.12
pytest>=7.0

# HTTP client — used by oanda + bridge sources
requests>=2.28

# ── Data sources (choose one) ────────────────────────────────────────────────

# Option A — OANDA (המומלץ: חינם לגמרי, שנים של נתוני M15/H1 אמיתיים)
#   1. https://www.oanda.com/register/#/sign-up/demo  (ללא כרטיס אשראי)
#   2. My Account → Manage API Access → Generate token
#   3. export OANDA_TOKEN=<token>
#   4. python main.py --symbol EURUSD --from 2023-01-01 --to 2024-01-01
#   (אין חבילה לדייב — OANDA API הוא HTTP ישיר, requests מספיק)

# Option B — yfinance (ללא הגדרה, אבל M15=60 יום בלבד)
yfinance>=0.2.28

# Option C — MT5 bridge server (mt5_bridge/server.py על Windows)
flask>=3.0

# Windows only — גישה ישירה ל-MT5
# MetaTrader5>=5.0

# ── Web dashboard (Streamlit) ─────────────────────────────────────────────────
streamlit>=1.30
plotly>=5.18
