pandas>=2.0
numpy>=1.24
pyarrow>=12.0
matplotlib>=3.7
mplfinance>=0.12
pytest>=7.0

# ── Mac/Linux data sources (choose one) ─────────────────────────────────────

# Option A — yfinance (zero setup, limited to 60d M15 / 730d H1):
yfinance>=0.2.28

# Option B — MetaApi (FREE cloud bridge to your MT5 demo, years of history):
#   1. Register: https://app.metaapi.cloud  (Community plan, no credit card)
#   2. Add your MT5 demo account credentials
#   3. export METAAPI_TOKEN=...  METAAPI_ACCOUNT_ID=...
metaapi-cloud-sdk>=27.0.0

# HTTP client (used by bridge + metaapi fallback):
requests>=2.28

# MT5 Bridge server (only on Windows machine running mt5_bridge/server.py):
flask>=3.0

# Windows only (direct MT5 connection):
# MetaTrader5>=5.0
