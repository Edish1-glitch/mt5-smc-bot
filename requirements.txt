pandas>=2.0
numpy>=1.24
pyarrow>=12.0
pytest>=7.0
pytz>=2023.3

# HTTP client — used by oanda + bridge sources
requests>=2.28

# ── Data sources ─────────────────────────────────────────────────────────────
# yfinance — works out of the box (M15=60d limit)
yfinance>=0.2.28

# Windows only — direct MT5 access
# MetaTrader5>=5.0

# ── Web dashboard (FastAPI + PWA) ────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
httpx>=0.27        # required by fastapi.testclient

# ── Optional: matplotlib for the legacy CLI --review flag ────────────────────
matplotlib>=3.7
mplfinance>=0.12
